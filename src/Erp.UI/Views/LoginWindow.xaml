<Window x:Class="Erp.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ERP - Login" Height="230" Width="360" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Usuário:" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Margin="6" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Text="Senha:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Margin="6" PasswordChanged="PasswordBox_OnPasswordChanged"/>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="6" Foreground="DarkRed" Text="{Binding Message}" TextWrapping="Wrap"/>

        <Button Grid.Row="3" Grid.ColumnSpan="2" Margin="6" Padding="8"
                Content="Entrar"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Grid>
</Window>
